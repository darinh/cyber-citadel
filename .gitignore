# Large/redistributable source (re-downloadable)
NIST.SP.800-53r5.pdf
# Python
.venv/
.venv_img/
.venv_tts/
__pycache__/
*.pyc
# Local image-model weights (IP-Adapter / SDXL helpers; re-downloadable)
tools/_ipa/
# Scratch/demo art
course/art/_demo/
# Persistent per-beat render artifacts (re-buildable; kept locally for incremental re-render)
course/render/
# Temp audio chunks
course/audio/_chunks/
tools/_tmp/
# Large local neural voice models (re-downloadable via piper.download_voices)
tools/voices/
# Large CC-BY source music tracks (re-downloadable via music2.ensure_assets); baked into videos
tools/music_assets/
# Audio work files (intermediate)
course/audio/*.wav
course/audio/*.mp3
# NOTE: course/episodes/*.mp4 + .srt/.vtt ARE committed so GitHub Pages can serve them.
# Node / Playwright web-test harness (tools/webtest is committed; deps + outputs are not)
node_modules/
test-results/
playwright-report/
.playwright/

